# Munmai Mobile

Minimal Expo Router foundation for Munmai's daily financial tracking workflow.

## Run locally

```bash
npm install
npm start
```

Use `npm run web`, `npm run android`, or `npm run ios` for a specific platform.

## Checks

```bash
npm run lint
npx expo export --platform web
```
